12. index.php OnKeyUp - обработчик события
	  myLibrary.js AJAX_Hint - get запрос на сервер
	  MyGetHint.php - формирование ответа в тексте поля hint

13* MyGetHint.php - если в полеченных данных присутствует поле цвет, добавляется <span style="color">
	  myLibrary.php  Colourize() добавление новой колонки при нажатии на кнопку
	  DB.php - получение данных из базы

16* Нажать на кнопку раскрасить
	  В предоставленном решении при повторном нажатии будет полученна ошибка о том что поле уже существует.
	  Для рещения проблемы нужно осуществить проверку.

17* При нажатии на кнопу add fruits фрукты добавляются без поля цвет. Для того чтоб заполнить цвета фруктов
	  в базе необходимо вызвать add fruits

Контрольные вопросы:
1.
	 В представленной работе проблема заключалась в том что при нажатии пользователем кнопки делался запрос в базу
	 данных с групировкой и оператором %like%. Данный запрос не являлся оптимизированным так как для его выполнения
	 необходимо было пройтись по всем записям в базе и произвести сложную операцию сравнения.

	 Для решения проблемы при создании таблитци был добавлен full text index позволяющий существенно снизить нагрузку
	 на базу при выполнении запроса. Недостатком данного подхода является увеличенное время для insert операций.

3. Ошибка с Duplicate column name, была устранена предварительной проверкой, на существование данной колонки и добавление её в противном случае.
	А именно запросом "SHOW COLUMNS FROM `MyTable`".

	Была добавленна проверка на существование колонки. Колонка добавляется только в том случае если она существует.
	Модификация колонок в базе является опасной операцией и не должна производится как реакция на действия пользователя.
	В идеале необходимо было бы сделать флаг отмечающий что пользователю нужно показывать цвета и при нажатии кнопки
	менять состояние флага.


	От себя:
	При выполнении работы вместо xampp были изученны возможности Docker для создания и взаимодействия контейнеров
	разных типов.

	Были изученны возможности создания одиночных контейнеров на основе образов
	при помощи командной строки а так же создание и запуск нескольких контейнеров на основе docker-compose.
	Рассмотренны поля позволяющие настраивать отдельные контейнеры.

	В дальнейшем было бы интересно изучить создание docker образов на основе dokcerfile
